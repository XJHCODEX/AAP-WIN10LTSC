---
- name: Controlled Windows reboot
  hosts: windows
  gather_facts: false
  tasks:
    - name: Reboot host
      ansible.windows.win_reboot:
        pre_reboot_delay: "{{ pre_reboot_delay_sec | default(30) | int }}"
        reboot_timeout: "{{ reboot_timeout_sec | default(600) | int }}"
